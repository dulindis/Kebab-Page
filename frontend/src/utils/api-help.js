import axios from 'axios';
 
// let one =  `/api/orders/${}`
// // cart.cartItems

// const requestOne = axios.post(`/api/orders/${item.id}`);


// const bulkUpdate=[];

// for( let item of cart.cartItems) {
//     bulkUpdate.push(axios.post(`/api/orders/${item.id}`))
// }

// axios.all(bulkUpdate).then(axios.spread((...responses) => {
//     const BulkRes=[]
//     for (let bulkReq of bulkUpdate) {
//         const bulkReq = responses[0]
//         const responseTwo = responses[1]
//         const responesThree = responses[2]
//         // use/access the results 
//     }
//     return BulkRes
//   })).catch(errors => {
//     // react on errors.
//   })

